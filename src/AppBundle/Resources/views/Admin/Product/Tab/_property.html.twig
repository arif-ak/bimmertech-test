{% form_theme form 'SyliusUiBundle:Form:imagesTheme.html.twig' %}
<div class="ui tab" data-tab="properties">
    <h3 class="ui dividing header">{{ 'sylius.ui.properties'|trans }}</h3>
    {{ form_row(form.properties) }}
    {{ sonata_block_render_event('sylius.admin.product.' ~ action ~ '.tab_properties', {'form': form}) }}

</div>
